# DevOps

GitHub collaboration and CI gates. Docker is **not** used for local dev (see `docker`, `local-dev`) — only a possible future deploy-time container. Cloud topology is `cloud/aws`, not here.
---
