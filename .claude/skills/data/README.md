# Data

Supabase is the database, auth, storage, and vector layer. Neo4j is the graph layer (RBAC + policy relationships) — see `graph`. Do not add RDS, a second vector product, or a second graph product.
---
